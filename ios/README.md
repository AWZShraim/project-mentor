# Mentor iOS

Native Swift/SwiftUI app. Requires macOS (Xcode) to build — see the "known gap" note in [../docs/technical_spec.md](../docs/technical_spec.md). Xcode project to be added once a Mac build environment is set up.
