# Mentor iOS

Native Swift/SwiftUI app. Requires macOS (Xcode) to build — see [../docs/technical_spec.md](../docs/technical_spec.md) for the MacinCloud setup decision. Xcode project to be added once the MacinCloud environment is set up: clone this repo there, create the SwiftUI app project in this folder, and push.
